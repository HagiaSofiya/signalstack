import { WorkspacePlaceholder } from "@/components/layout/workspace-placeholder";

export default function DatasetsPage() {
  return <WorkspacePlaceholder title="Datasets" description="Browse and manage the files connected to your projects." />;
}
