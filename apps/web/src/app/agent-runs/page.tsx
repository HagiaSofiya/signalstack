import { WorkspacePlaceholder } from "@/components/layout/workspace-placeholder";

export default function AgentRunsPage() {
  return <WorkspacePlaceholder title="Agent Runs" description="Review questions, tool steps, and answers produced by the agent." />;
}
