import { WorkspacePlaceholder } from "@/components/layout/workspace-placeholder";

export default function ProjectsPage() {
  return <WorkspacePlaceholder title="Projects" description="Organize datasets and agent runs into focused workspaces." />;
}
